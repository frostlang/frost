/*

vec : const type = struct {
}

v : vec

should compile to 

struct vec {};

struct vec v;

*/


vec : const type = struct {
    x : mut u32
    y : const u32
}
main : pub const fn = (){    
    a : vec
    b : vec
    c : vec
}
